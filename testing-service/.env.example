PORT=5000
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/dexmy
# Must be exactly the same secret and algorithm configured in the FastAPI service.
JWT_SECRET_KEY=replace-with-the-same-production-secret-used-by-fastapi
JWT_ALGORITHM=HS256
NODE_ENV=development
