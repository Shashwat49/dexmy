// Placeholder — real parent dashboard (ported from parent-dashboard.html)
// gets wired up with live data in the next step.
export default function ParentDashboard() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-chalk-muted text-sm">Parent dashboard coming in the next step.</p>
    </div>
  );
}
