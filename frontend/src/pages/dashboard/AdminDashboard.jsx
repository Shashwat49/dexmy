// Placeholder — real admin dashboard (ported from admin-dashboard.html)
// gets wired up with live data in the next step.
export default function AdminDashboard() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-chalk-muted text-sm">Admin dashboard coming in the next step.</p>
    </div>
  );
}
