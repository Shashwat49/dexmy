import { useEffect, useState } from "react";
import DashboardLayout from "../../components/dashboard/DashboardLayout";
import api from "../../api/client";
export default function AdminAuditLogs(){const[logs,setLogs]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState("");useEffect(()=>{(async()=>{try{const r=await api.get("/admin/audit-logs",{params:{limit:200}});setLogs(r.data||[])}catch(e){setError(e.response?.data?.detail||"Unable to load audit logs.")}finally{setLoading(false)}})()},[]);return <DashboardLayout><div className="border-b border-chalk-faint px-8 py-5.5"><h1 className="font-display text-2xl">Audit logs</h1><p className="mt-1 text-sm text-chalk-muted">Review administrative actions and recorded changes.</p></div><div className="flex-1 overflow-auto px-8 py-7">{error&&<div className="mb-5 rounded-xl border border-brand-red/30 bg-brand-red/10 px-5 py-4 text-sm text-brand-red">{error}</div>}{loading?<p className="text-sm text-chalk-muted">Loading audit logs…</p>:<div className="overflow-x-auto rounded-xl border border-chalk-faint bg-panel-2"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-chalk-faint text-xs uppercase tracking-wide text-chalk-muted"><tr><th className="px-5 py-4">Time</th><th className="px-5 py-4">Action</th><th className="px-5 py-4">Resource</th><th className="px-5 py-4">Reason</th><th className="px-5 py-4">Changes</th></tr></thead><tbody className="divide-y divide-chalk-faint">{logs.map(log=><tr key={log.id}><td className="px-5 py-4 whitespace-nowrap">{new Date(log.created_at).toLocaleString()}</td><td className="px-5 py-4 font-medium">{log.action}</td><td className="px-5 py-4">{log.resource_type}{log.resource_id&&<span className="block font-mono text-xs text-chalk-muted">{log.resource_id}</span>}</td><td className="px-5 py-4 text-chalk-muted">{log.reason||"—"}</td><td className="px-5 py-4 text-xs"><div>Before: {log.old_values?JSON.stringify(log.old_values):"—"}</div><div className="mt-1">After: {log.new_values?JSON.stringify(log.new_values):"—"}</div></td></tr>)}</tbody></table>{logs.length===0&&<div className="px-6 py-10 text-sm text-chalk-muted">No audit entries found.</div>}</div>}</div></DashboardLayout>}
