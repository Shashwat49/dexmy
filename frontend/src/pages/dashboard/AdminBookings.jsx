import { useEffect, useState } from "react";
import DashboardLayout from "../../components/dashboard/DashboardLayout";
import api from "../../api/client";

export default function AdminBookings(){const[items,setItems]=useState([]),[status,setStatus]=useState(""),[loading,setLoading]=useState(true),[error,setError]=useState(""),[assigning,setAssigning]=useState(null),[teacherLists,setTeacherLists]=useState({}),[selected,setSelected]=useState({});
async function load(){setLoading(true);setError("");try{const r=await api.get("/admin/bookings",{params:{page:1,page_size:100,...(status?{status}: {})}});setItems(r.data?.items||[])}catch(e){setError(e.response?.data?.detail||"Unable to load bookings.")}finally{setLoading(false)}}useEffect(()=>{load()},[status]);
async function openAssign(b){setAssigning(b.id);setError("");try{const r=await api.get(`/admin/bookings/${b.id}/eligible-teachers`);setTeacherLists(x=>({...x,[b.id]:r.data||[]}));setSelected(x=>({...x,[b.id]:""}))}catch(e){setError(e.response?.data?.detail||"Unable to load eligible teachers.")}finally{setAssigning(null)}}
async function assign(b){const teacherId=selected[b.id];if(!teacherId)return;const ok=window.confirm("Assign this teacher to the booking?");if(!ok)return;setAssigning(b.id);setError("");try{await api.post(`/admin/bookings/${b.id}/assign-teacher`,{teacher_id:teacherId});const chosen=(teacherLists[b.id]||[]).find(t=>t.id===teacherId);setItems(x=>x.map(row=>row.id===b.id?{...row,teacher_id:teacherId,teacher_name:chosen?.full_name||"Assigned",teacher_assignment_status:"assigned"}:row));setTeacherLists(x=>({...x,[b.id]:undefined}));setSelected(x=>({...x,[b.id]:undefined}))}catch(e){setError(e.response?.data?.detail||"Unable to assign teacher.")}finally{setAssigning(null)}}
return <DashboardLayout><div className="border-b border-chalk-faint px-8 py-5.5"><h1 className="font-display text-2xl">Bookings</h1><p className="mt-1 text-sm text-chalk-muted">Monitor class schedules and assign eligible teachers.</p></div><div className="flex-1 overflow-auto px-8 py-7">{error&&<div className="mb-5 rounded-xl border border-brand-red/30 bg-brand-red/10 px-5 py-4 text-sm text-brand-red">{error}</div>}<div className="mb-6"><select value={status} onChange={e=>setStatus(e.target.value)} className="rounded-lg border border-chalk-faint bg-panel px-3 py-2 text-sm"><option value="">All statuses</option><option value="scheduled">Scheduled</option><option value="confirmed">Confirmed</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option></select></div>{loading?<p className="text-sm text-chalk-muted">Loading bookings…</p>:<div className="overflow-x-auto rounded-xl border border-chalk-faint bg-panel-2"><table className="w-full min-w-[1200px] text-left text-sm"><thead className="border-b border-chalk-faint text-xs uppercase tracking-wide text-chalk-muted"><tr><th className="px-5 py-4">Student</th><th className="px-5 py-4">Subject</th><th className="px-5 py-4">Teacher</th><th className="px-5 py-4">Schedule</th><th className="px-5 py-4">Status</th><th className="px-5 py-4">Assignment</th><th className="px-5 py-4">Price</th><th className="px-5 py-4">Action</th></tr></thead><tbody className="divide-y divide-chalk-faint">{items.map(b=>{const candidates=teacherLists[b.id]||[];return <tr key={b.id}><td className="px-5 py-4"><div className="font-semibold">{b.student_name}</div><div className="text-xs text-chalk-muted">{b.student_id}</div></td><td className="px-5 py-4">{b.subject_name}</td><td className="px-5 py-4">{b.teacher_name||"Unassigned"}</td><td className="px-5 py-4">{new Date(b.scheduled_at).toLocaleString()}<div className="text-xs text-chalk-muted">{b.duration_minutes} min</div></td><td className="px-5 py-4">{b.status}</td><td className="px-5 py-4">{b.teacher_assignment_status||"—"}</td><td className="px-5 py-4">{b.price==null?"—":b.price}</td><td className="px-5 py-4">{!b.teacher_id&&b.status==="confirmed"?<div className="min-w-[280px]">{teacherLists[b.id]!==undefined?<><div className="mb-2 text-xs text-chalk-muted">{candidates.filter(t=>t.is_available).length} available / {candidates.length} eligible</div><div className="flex items-center gap-2"><select value={selected[b.id]||""} onChange={e=>setSelected(x=>({...x,[b.id]:e.target.value}))} className="max-w-[210px] rounded-lg border border-chalk-faint bg-panel px-2 py-2 text-xs"><option value="">Select available teacher</option>{candidates.map(t=><option key={t.id} value={t.id} disabled={!t.is_available}>{t.full_name}{t.is_available?" — Available":" — Busy"}</option>)}</select><button disabled={!selected[b.id]||assigning===b.id} onClick={()=>assign(b)} className="rounded-lg bg-brand-red px-3 py-2 text-xs font-semibold disabled:opacity-50">Assign</button></div>{candidates.length===0&&<div className="text-xs text-brand-red">No eligible teachers found.</div>}</>:<button disabled={assigning===b.id} onClick={()=>openAssign(b)} className="rounded-lg border border-chalk-faint px-3 py-2 text-xs font-semibold hover:border-brand-red">{assigning===b.id?"Loading…":"View Eligible Teachers"}</button>}</div>:b.teacher_id?<span className="text-xs text-chalk-muted">Assigned</span>:"—"}</td></tr>})}</tbody></table>{items.length===0&&<div className="px-6 py-10 text-sm text-chalk-muted">No bookings found.</div>}</div>}</div></DashboardLayout>}
