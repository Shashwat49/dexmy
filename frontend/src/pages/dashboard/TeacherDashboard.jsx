// Placeholder — real teacher dashboard (ported from teacher-dashboard.html)
// gets wired up with live data in the next step.
export default function TeacherDashboard() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-chalk-muted text-sm">Teacher dashboard coming in the next step.</p>
    </div>
  );
}
