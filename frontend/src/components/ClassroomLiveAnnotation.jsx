import ClassroomPro from "../pages/ClassroomPro";

// Keep the original classroom UI/layout. Live annotation experiments are
// intentionally disabled; ClassroomPro remains the single whiteboard source.
export default function ClassroomLiveAnnotation() {
  return <ClassroomPro />;
}
