name: Whiteboard shape fix
on:
  push:
    branches:
      - fix/whiteboard-shape-preview-final
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test runner
        run: echo "whiteboard patch runner is active"
